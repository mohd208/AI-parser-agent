FROM golang:{{ runtime.version }}-alpine AS build
WORKDIR /app
COPY go.mod go.sum ./
RUN go mod download
COPY . .
RUN CGO_ENABLED=0 go build -o /app/server .

FROM alpine:3.20
RUN adduser -D app
COPY --from=build /app/server /server
USER app
EXPOSE {{ runtime.port }}
ENTRYPOINT ["/server"]
